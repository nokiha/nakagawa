<% @page_title = "予約ページ" %>
<h1><%= @page_title %></h1>

<ul>
<p>診察を希望する先生を選択してください</p>
</ul>

<table class="list">
<% @doctors.each do |doctor| %>
  <tr>
    <td><%= link_to doctor.username + "先生", patient_doctor_slots_path(doctor_id: doctor.id) %></td>
  </tr>
<% end %>
</table>